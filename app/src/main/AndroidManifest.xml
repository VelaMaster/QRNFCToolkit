<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camara: requerida para escanear codigos QR -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
        Ubicacion: requerida en Android 9+ para leer el SSID de la red WiFi actual
        (funcion de autocompletar SSID en generacion de QR/NFC WiFi).
        ACCESS_COARSE_LOCATION es suficiente para operaciones WiFi; se mantiene
        ACCESS_FINE_LOCATION solo por compatibilidad con la API WifiManager en API 28.
        En Android 13+ (API 33) se puede reemplazar por NEARBY_WIFI_DEVICES.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Estado WiFi: para leer SSID de la red actual -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Estado de red: para verificar conectividad -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- NFC: lectura y escritura de tags -->
    <uses-permission android:name="android.permission.NFC" />

    <uses-feature android:name="android.hardware.camera.any" />
    <!-- NFC no requerido: la app funciona sin NFC (funcionalidad QR sigue disponible) -->
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.QRNFCToolkit">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.QRNFCToolkit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
